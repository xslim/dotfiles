{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "brew bundle install",
            "type": "shell",
            "command": "brew bundle install -v"
        },
        {
            "label": "rerun dock.sh",
            "type": "shell",
            "command": "sh scripts/dock.sh"
        }
    ]
}